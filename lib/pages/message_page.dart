import 'package:flutter/material.dart';

class MesagePage extends StatelessWidget {
  const MesagePage({Key? key}) : super(key: key);

  @override
  Widget build(BuildContext context) {
    return Container(
      child: Center(
        child: Text("Message Page"),
      ),
    );
  }
}
